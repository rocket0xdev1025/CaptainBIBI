(() => {
  "use strict";
  const e = {};
  function t() {
    if (location.hash) return location.hash.replace("#", "");
  }
  let s = (e, t = 500, s = 0) => {
      e.classList.contains("_slide") ||
        (e.classList.add("_slide"),
        (e.style.transitionProperty = "height, margin, padding"),
        (e.style.transitionDuration = t + "ms"),
        (e.style.height = `${e.offsetHeight}px`),
        e.offsetHeight,
        (e.style.overflow = "hidden"),
        (e.style.height = s ? `${s}px` : "0px"),
        (e.style.paddingTop = 0),
        (e.style.paddingBottom = 0),
        (e.style.marginTop = 0),
        (e.style.marginBottom = 0),
        window.setTimeout(() => {
          (e.hidden = !s),
            !s && e.style.removeProperty("height"),
            e.style.removeProperty("padding-top"),
            e.style.removeProperty("padding-bottom"),
            e.style.removeProperty("margin-top"),
            e.style.removeProperty("margin-bottom"),
            !s && e.style.removeProperty("overflow"),
            e.style.removeProperty("transition-duration"),
            e.style.removeProperty("transition-property"),
            e.classList.remove("_slide"),
            document.dispatchEvent(
              new CustomEvent("slideUpDone", { detail: { target: e } })
            );
        }, t));
    },
    o = (e, t = 500, s = 0) => {
      if (!e.classList.contains("_slide")) {
        e.classList.add("_slide"),
          (e.hidden = !e.hidden && null),
          s && e.style.removeProperty("height");
        let o = e.offsetHeight;
        (e.style.overflow = "hidden"),
          (e.style.height = s ? `${s}px` : "0px"),
          (e.style.paddingTop = 0),
          (e.style.paddingBottom = 0),
          (e.style.marginTop = 0),
          (e.style.marginBottom = 0),
          e.offsetHeight,
          (e.style.transitionProperty = "height, margin, padding"),
          (e.style.transitionDuration = t + "ms"),
          (e.style.height = o + "px"),
          e.style.removeProperty("padding-top"),
          e.style.removeProperty("padding-bottom"),
          e.style.removeProperty("margin-top"),
          e.style.removeProperty("margin-bottom"),
          window.setTimeout(() => {
            e.style.removeProperty("height"),
              e.style.removeProperty("overflow"),
              e.style.removeProperty("transition-duration"),
              e.style.removeProperty("transition-property"),
              e.classList.remove("_slide"),
              document.dispatchEvent(
                new CustomEvent("slideDownDone", { detail: { target: e } })
              );
          }, t);
      }
    },
    a = !0,
    n = (e = 500) => {
      if (a) {
        const t = document.querySelectorAll("[data-lp]");
        setTimeout(() => {
          t.forEach((e) => {
            e.style.paddingRight = "";
          }),
            (document.body.style.paddingRight = ""),
            document.documentElement.classList.remove("lock"),
            window.onRefreshLenisScroll && window.onRefreshLenisScroll();
        }, e),
          (a = !1),
          setTimeout(function () {
            a = !0;
          }, e);
      }
    },
    i = (e = 500) => {
      if (a) {
        const t = document.querySelectorAll("[data-lp]"),
          s = window.innerWidth - document.body.offsetWidth + "px";
        t.forEach((e) => {
          e.style.paddingRight = s;
        }),
          (document.body.style.paddingRight = s),
          document.documentElement.classList.add("lock"),
          window.lenis?.destroy(),
          (a = !1),
          setTimeout(function () {
            a = !0;
          }, e);
      }
    };
  function r() {
    n(),
      document.documentElement.classList.add("menu-close"),
      document.documentElement.classList.remove("menu-open");
  }
  function l(e) {
    setTimeout(() => {
      window.FLS && console.log(e);
    }, 0);
  }
  function c(e) {
    return e.filter(function (e, t, s) {
      return s.indexOf(e) === t;
    });
  }
  function d(e, t) {
    const s = Array.from(e).filter(function (e, s, o) {
      if (e.dataset[t]) return e.dataset[t].split(",")[0];
    });
    if (s.length) {
      const e = [];
      s.forEach((s) => {
        const o = {},
          a = s.dataset[t].split(",");
        (o.value = a[0]),
          (o.type = a[1] ? a[1].trim() : "max"),
          (o.item = s),
          e.push(o);
      });
      let o = e.map(function (e) {
        return (
          "(" + e.type + "-width: " + e.value + "px)," + e.value + "," + e.type
        );
      });
      o = c(o);
      const a = [];
      if (o.length)
        return (
          o.forEach((t) => {
            const s = t.split(","),
              o = s[1],
              n = s[2],
              i = window.matchMedia(s[0]),
              r = e.filter(function (e) {
                if (e.value === o && e.type === n) return !0;
              });
            a.push({ itemsArray: r, matchMedia: i });
          }),
          a
        );
    }
  }
  if (!window.navigator.userAgent.match(/Trident\/7\./, [])) {
    const e = document.querySelectorAll("video");
    !(function () {
      const e = window.navigator,
        t = e.userAgent.toLowerCase(),
        s = !(!e.mediaCapabilities || !e.mediaCapabilities.decodingInfo);
      return (
        -1 != t.indexOf("safari") &&
        !(-1 != t.indexOf("chrome")) &&
        -1 != t.indexOf("version/") &&
        s
      );
    })()
      ? e.forEach((e) => {
          let t = e.getAttribute("src");
          (t += "webm"), e.setAttribute("src", t);
        })
      : e.forEach((e) => {
          let t = e.getAttribute("src");
          (t += "mov"),
            e.setAttribute("src", t),
            e.setAttribute("type", "video/quicktime");
        });
  }
  let h = (e, t = !1, s = 500, o = 0) => {
    const a = document.querySelector(e);
    if (a) {
      let n = "",
        i = 0;
      if (t) {
        n = "header.header";
        const e = document.querySelector(n);
        e.classList.contains("_header-scroll")
          ? (i = e.offsetHeight)
          : ((e.style.cssText = "transition-duration: 0s;"),
            e.classList.add("_header-scroll"),
            (i = e.offsetHeight),
            e.classList.remove("_header-scroll"),
            setTimeout(() => {
              e.style.cssText = "";
            }, 0));
      }
      let c = {
        speedAsDuration: !0,
        speed: s,
        header: n,
        offset: o,
        easing: "easeOutQuad",
      };
      if (
        (document.documentElement.classList.contains("menu-open") && r(),
        "undefined" != typeof SmoothScroll)
      )
        new SmoothScroll().animateScroll(a, "", c);
      else {
        let e = a.getBoundingClientRect().top + scrollY;
        (e = i ? e - i : e),
          (e = o ? e - o : e),
          window.scrollTo({ top: e, behavior: "smooth" });
      }
      l(`[gotoBlock]: Юхуу...їдемо до ${e}`);
    } else l(`[gotoBlock]: Йой... Такого блоку немає на сторінці: ${e}`);
  };
  e.watcher = new (class {
    constructor(e) {
      (this.config = Object.assign({ logging: !0 }, e)),
        this.observer,
        !document.documentElement.classList.contains("watcher") &&
          this.scrollWatcherRun();
    }
    scrollWatcherUpdate() {
      this.scrollWatcherRun();
    }
    scrollWatcherRun() {
      document.documentElement.classList.add("watcher"),
        this.scrollWatcherConstructor(
          document.querySelectorAll("[data-watch]")
        );
    }
    scrollWatcherConstructor(e) {
      if (e.length) {
        this.scrollWatcherLogging(
          `Прокинувся, стежу за об'єктами (${e.length})...`
        ),
          c(
            Array.from(e).map(function (e) {
              if (
                "navigator" === e.dataset.watch &&
                !e.dataset.watchThreshold
              ) {
                let t;
                e.clientHeight > 2
                  ? ((t = window.innerHeight / 2 / (e.clientHeight - 1)),
                    t > 1 && (t = 1))
                  : (t = 1),
                  e.setAttribute("data-watch-threshold", t.toFixed(2));
              }
              return `${
                e.dataset.watchRoot ? e.dataset.watchRoot : null
              }|${e.dataset.watchMargin ? e.dataset.watchMargin : "0px"}|${e.dataset.watchThreshold ? e.dataset.watchThreshold : 0}`;
            })
          ).forEach((t) => {
            let s = t.split("|"),
              o = { root: s[0], margin: s[1], threshold: s[2] },
              a = Array.from(e).filter(function (e) {
                let t = e.dataset.watchRoot ? e.dataset.watchRoot : null,
                  s = e.dataset.watchMargin ? e.dataset.watchMargin : "0px",
                  a = e.dataset.watchThreshold ? e.dataset.watchThreshold : 0;
                if (
                  String(t) === o.root &&
                  String(s) === o.margin &&
                  String(a) === o.threshold
                )
                  return e;
              }),
              n = this.getScrollWatcherConfig(o);
            this.scrollWatcherInit(a, n);
          });
      } else
        this.scrollWatcherLogging("Сплю, немає об'єктів для стеження. ZzzZZzz");
    }
    getScrollWatcherConfig(e) {
      let t = {};
      if (
        (document.querySelector(e.root)
          ? (t.root = document.querySelector(e.root))
          : "null" !== e.root &&
            this.scrollWatcherLogging(
              `Эмм... батьківського об'єкта ${e.root} немає на сторінці`
            ),
        (t.rootMargin = e.margin),
        !(e.margin.indexOf("px") < 0 && e.margin.indexOf("%") < 0))
      ) {
        if ("prx" === e.threshold) {
          e.threshold = [];
          for (let t = 0; t <= 1; t += 0.005) e.threshold.push(t);
        } else e.threshold = e.threshold.split(",");
        return (t.threshold = e.threshold), t;
      }
      this.scrollWatcherLogging(
        "йой, налаштування data-watch-margin потрібно задавати в PX або %"
      );
    }
    scrollWatcherCreate(e) {
      this.observer = new IntersectionObserver((e, t) => {
        e.forEach((e) => {
          this.scrollWatcherCallback(e, t);
        });
      }, e);
    }
    scrollWatcherInit(e, t) {
      this.scrollWatcherCreate(t), e.forEach((e) => this.observer.observe(e));
    }
    scrollWatcherIntersecting(e, t) {
      e.isIntersecting
        ? (!t.classList.contains("_watcher-view") &&
            t.classList.add("_watcher-view"),
          this.scrollWatcherLogging(
            `Я бачу ${t.classList}, додав клас _watcher-view`
          ))
        : (t.classList.contains("_watcher-view") &&
            t.classList.remove("_watcher-view"),
          this.scrollWatcherLogging(
            `Я не бачу ${t.classList}, прибрав клас _watcher-view`
          ));
    }
    scrollWatcherOff(e, t) {
      t.unobserve(e),
        this.scrollWatcherLogging(`Я перестав стежити за ${e.classList}`);
    }
    scrollWatcherLogging(e) {
      this.config.logging && l(`[Спостерігач]: ${e}`);
    }
    scrollWatcherCallback(e, t) {
      const s = e.target;
      this.scrollWatcherIntersecting(e, s),
        s.hasAttribute("data-watch-once") &&
          e.isIntersecting &&
          this.scrollWatcherOff(s, t),
        document.dispatchEvent(
          new CustomEvent("watcherCallback", { detail: { entry: e } })
        );
    }
  })({});
  let m = !1;
  setTimeout(() => {
    if (m) {
      let e = new Event("windowScroll");
      window.addEventListener("scroll", function (t) {
        document.dispatchEvent(e);
      });
    }
  }, 0);
  new (class {
    constructor(e) {
      this.type = e;
    }
    init() {
      (this.оbjects = []),
        (this.daClassname = "_dynamic_adapt_"),
        (this.nodes = [...document.querySelectorAll("[data-da]")]),
        this.nodes.forEach((e) => {
          const t = e.dataset.da.trim().split(","),
            s = {};
          (s.element = e),
            (s.parent = e.parentNode),
            (s.destination = document.querySelector(`${t[0].trim()}`)),
            (s.breakpoint = t[1] ? t[1].trim() : "767.98"),
            (s.place = t[2] ? t[2].trim() : "last"),
            (s.index = this.indexInParent(s.parent, s.element)),
            this.оbjects.push(s);
        }),
        this.arraySort(this.оbjects),
        (this.mediaQueries = this.оbjects
          .map(({ breakpoint: e }) => `(${this.type}-width: ${e / 16}em),${e}`)
          .filter((e, t, s) => s.indexOf(e) === t)),
        this.mediaQueries.forEach((e) => {
          const t = e.split(","),
            s = window.matchMedia(t[0]),
            o = t[1],
            a = this.оbjects.filter(({ breakpoint: e }) => e === o);
          s.addEventListener("change", () => {
            this.mediaHandler(s, a);
          }),
            this.mediaHandler(s, a);
        });
    }
    mediaHandler(e, t) {
      e.matches
        ? t.forEach((e) => {
            this.moveTo(e.place, e.element, e.destination);
          })
        : t.forEach(({ parent: e, element: t, index: s }) => {
            t.classList.contains(this.daClassname) && this.moveBack(e, t, s);
          });
    }
    moveTo(e, t, s) {
      t.classList.add(this.daClassname),
        "last" === e || e >= s.children.length
          ? s.append(t)
          : "first" !== e
          ? s.children[e].before(t)
          : s.prepend(t);
    }
    moveBack(e, t, s) {
      t.classList.remove(this.daClassname),
        void 0 !== e.children[s] ? e.children[s].before(t) : e.append(t);
    }
    indexInParent(e, t) {
      return [...e.children].indexOf(t);
    }
    arraySort(e) {
      "min" === this.type
        ? e.sort((e, t) =>
            e.breakpoint === t.breakpoint
              ? e.place === t.place
                ? 0
                : "first" === e.place || "last" === t.place
                ? -1
                : "last" === e.place || "first" === t.place
                ? 1
                : 0
              : e.breakpoint - t.breakpoint
          )
        : e.sort((e, t) =>
            e.breakpoint === t.breakpoint
              ? e.place === t.place
                ? 0
                : "first" === e.place || "last" === t.place
                ? 1
                : "last" === e.place || "first" === t.place
                ? -1
                : 0
              : t.breakpoint - e.breakpoint
          );
    }
  })("max").init();
  const u = document.querySelector(".form__result-message");
  document.getElementById("form").addEventListener("submit", function (e) {
    e.preventDefault();
    let t = "<b>New message from site</b>\n";
    (t += `<b>User Name: </b> ${this.userName.value}\n`),
      (t += `<b>User Email: </b> ${this.userEmail.value}`),
      axios
        .post(
          "https://api.telegram.org/bot7065999726:AAFOUjdsdUzv1TQEcRUy23JzY_3wVIChJ3k/sendMessage",
          { chat_id: "-1002280401717", parse_mode: "html", text: t }
        )
        .then((e) => {
          (this.userName.value = ""),
            (this.userEmail.value = ""),
            (u.innerText = "Your information has been successfully sent"),
            (u.style.backgroundColor = "#90ee90"),
            u.classList.remove("hidden"),
            setTimeout(() => {
              u.classList.add("hidden");
            }, 6e3);
        })
        .catch((e) => {
          (u.innerText = "Something went wrong. Please try again."),
            (u.style.backgroundColor = "#ff4f4f"),
            u.classList.remove("hidden"),
            setTimeout(() => {
              u.classList.add("hidden");
            }, 6e3);
        });
  }),
    document.documentElement.classList.contains("loading") ||
      window.addEventListener("load", function () {
        setTimeout(function () {
          document.documentElement.classList.add("loaded");
        }, 0);
      }),
    document.querySelector(".icon-menu") &&
      document.addEventListener("click", function (e) {
        a &&
          e.target.closest(".icon-menu") &&
          (((e = 500) => {
            document.documentElement.classList.contains("lock") ? n(e) : i(e);
          })(),
          document.documentElement.classList.contains("menu-open")
            ? r()
            : (i(),
              document.documentElement.classList.add("menu-open"),
              document.documentElement.classList.remove("menu-close")));
      }),
    (function () {
      const e = document.querySelectorAll("[data-spollers]");
      if (e.length > 0) {
        document.addEventListener("click", r);
        const t = Array.from(e).filter(function (e, t, s) {
          return !e.dataset.spollers.split(",")[0];
        });
        t.length && n(t);
        let a = d(e, "spollers");
        function n(e, t = !1) {
          e.forEach((e) => {
            (e = t ? e.item : e),
              t.matches || !t
                ? (e.classList.add("_spoller-init"), i(e))
                : (e.classList.remove("_spoller-init"), i(e, !1));
          });
        }
        function i(e, t = !0) {
          let s = e.querySelectorAll("details");
          s.length &&
            s.forEach((e) => {
              let s = e.querySelector("summary");
              t
                ? (s.removeAttribute("tabindex"),
                  e.hasAttribute("data-open")
                    ? (s.classList.add("_spoller-active"), (e.open = !0))
                    : ((e.open = !1), (s.nextElementSibling.hidden = !0)))
                : (s.setAttribute("tabindex", "-1"),
                  s.classList.remove("_spoller-active"),
                  (e.open = !0),
                  (s.nextElementSibling.hidden = !1));
            });
        }
        function r(e) {
          const t = e.target;
          if (
            t.closest("summary") &&
            t.closest("[data-spollers]") &&
            (e.preventDefault(),
            t.closest("[data-spollers]").classList.contains("_spoller-init"))
          ) {
            const e = t.closest("summary"),
              a = e.closest("details"),
              n = e.closest("[data-spollers]"),
              i = n.hasAttribute("data-one-spoller"),
              r = a.hasAttribute("data-spoller-scroll"),
              c = n.dataset.spollersSpeed
                ? parseInt(n.dataset.spollersSpeed)
                : 500;
            if (
              !n.querySelectorAll("._slide").length &&
              (i && !a.open && l(n),
              a.open
                ? setTimeout(() => {
                    a.open = !1;
                  }, c)
                : (a.open = !0),
              e.classList.toggle("_spoller-active"),
              ((e, t = 500) => {
                e.hidden ? o(e, t) : s(e, t);
              })(e.nextElementSibling, c),
              r && e.classList.contains("_spoller-active"))
            ) {
              const e = a.dataset.spollerScroll,
                t = +e ? +e : 0,
                s = a.hasAttribute("data-spoller-scroll-noheader")
                  ? document.querySelector(".header").offsetHeight
                  : 0;
              window.scrollTo({
                top: a.offsetTop - (t + s),
                behavior: "smooth",
              });
            }
          }
          if (!t.closest("[data-spollers]")) {
            const e = document.querySelectorAll("[data-spoller-close]");
            e.length &&
              e.forEach((e) => {
                const t = e.closest("[data-spollers]"),
                  o = e.parentNode;
                if (t.classList.contains("_spoller-init")) {
                  const a = t.dataset.spollersSpeed
                    ? parseInt(t.dataset.spollersSpeed)
                    : 500;
                  e.classList.remove("_spoller-active"),
                    s(e.nextElementSibling, a),
                    setTimeout(() => {
                      o.open = !1;
                    }, a);
                }
              });
          }
        }
        function l(e) {
          const t = e.querySelector("details[open]");
          if (t && !e.querySelectorAll("._slide").length) {
            const o = t.querySelector("summary"),
              a = e.dataset.spollersSpeed
                ? parseInt(e.dataset.spollersSpeed)
                : 500;
            o.classList.remove("_spoller-active"),
              s(o.nextElementSibling, a),
              setTimeout(() => {
                t.open = !1;
              }, a);
          }
        }
        a &&
          a.length &&
          a.forEach((e) => {
            e.matchMedia.addEventListener("change", function () {
              n(e.itemsArray, e.matchMedia);
            }),
              n(e.itemsArray, e.matchMedia);
          });
      }
    })(),
    (function () {
      const e = document.querySelectorAll("[data-clipboardBtn]");
      e.length &&
        e.forEach((e) => {
          e.addEventListener("click", () => {
            const t = e.closest("[data-clipboard]");
            if (t) {
              const s = t
                  .querySelector("[data-clipboardEl]")
                  .textContent.trim(),
                o = t.querySelector("[data-cnHint]");
              async function a(e) {
                try {
                  await navigator.clipboard.writeText(e);
                } catch (e) {
                  o.textContent = "Doesn't copied";
                } finally {
                  setTimeout(() => {
                    t.classList.remove("active");
                  }, 1e3);
                }
              }
              t.classList.add("active"), a(s);
            }
          });
        });
    })(),
    (function () {
      function s(t) {
        if ("click" === t.type) {
          const s = t.target;
          if (s.closest("[data-goto]")) {
            const o = s.closest("[data-goto]"),
              a = o.dataset.goto ? o.dataset.goto : "",
              n = !!o.hasAttribute("data-goto-header"),
              i = o.dataset.gotoSpeed ? o.dataset.gotoSpeed : 500,
              l = o.dataset.gotoTop ? parseInt(o.dataset.gotoTop) : 0;
            if (e.fullpage) {
              const t = document
                  .querySelector(`${a}`)
                  .closest("[data-fp-section]"),
                s = t ? +t.dataset.fpId : null;
              null !== s &&
                (e.fullpage.switchingSection(s),
                document.documentElement.classList.contains("menu-open") &&
                  r());
            } else h(a, n, i, l);
            t.preventDefault();
          }
        } else if ("watcherCallback" === t.type && t.detail) {
          const e = t.detail.entry,
            s = e.target;
          if ("navigator" === s.dataset.watch) {
            document.querySelector("[data-goto]._navigator-active");
            let t;
            if (s.id && document.querySelector(`[data-goto="#${s.id}"]`))
              t = document.querySelector(`[data-goto="#${s.id}"]`);
            else if (s.classList.length)
              for (let e = 0; e < s.classList.length; e++) {
                const o = s.classList[e];
                if (document.querySelector(`[data-goto=".${o}"]`)) {
                  t = document.querySelector(`[data-goto=".${o}"]`);
                  break;
                }
              }
            e.isIntersecting
              ? t && t.classList.add("_navigator-active")
              : t && t.classList.remove("_navigator-active");
          }
        }
      }
      if (
        (document.addEventListener("click", s),
        document.addEventListener("watcherCallback", s),
        t())
      ) {
        let e;
        document.querySelector(`#${t()}`)
          ? (e = `#${t()}`)
          : document.querySelector(`.${t()}`) && (e = `.${t()}`),
          e && h(e, !0, 500, 20);
      }
    })(),
    (function () {
      m = !0;
      const e = document.querySelector("header.header"),
        t = e.hasAttribute("data-scroll-show"),
        s = e.dataset.scrollShow ? e.dataset.scrollShow : 500,
        o = e.dataset.scroll ? e.dataset.scroll : 1;
      let a,
        n = 0;
      document.addEventListener("windowScroll", function (i) {
        const r = window.scrollY;
        clearTimeout(a),
          r >= o
            ? (!e.classList.contains("_header-scroll") &&
                e.classList.add("_header-scroll"),
              t &&
                (r > n
                  ? e.classList.contains("_header-show") &&
                    e.classList.remove("_header-show")
                  : !e.classList.contains("_header-show") &&
                    e.classList.add("_header-show"),
                (a = setTimeout(() => {
                  !e.classList.contains("_header-show") &&
                    e.classList.add("_header-show");
                }, s))))
            : (e.classList.contains("_header-scroll") &&
                e.classList.remove("_header-scroll"),
              t &&
                e.classList.contains("_header-show") &&
                e.classList.remove("_header-show")),
          (n = r <= 0 ? 0 : r);
      });
    })();
})();
